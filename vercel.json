{
  "buildCommand": "pnpm --filter @workspace/rahul-agro run build && rm -rf public && cp -r artifacts/rahul-agro/dist/public public",
  "installCommand": "pnpm install --frozen-lockfile",
  "outputDirectory": "public",
  "framework": "vite",
  "env": {
    "PORT": "5173",
    "BASE_PATH": "/"
  }
}
